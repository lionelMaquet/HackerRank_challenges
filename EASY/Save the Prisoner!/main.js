// n = number of prisoners 
// m = number of candies
// s = seat to start counting from (starting at 1)

function saveThePrisoner(n, m, s) {

    let numCand = m, numPris = n, start = s;

    return numCand + (Start % (numPris+1))+1

}

console.log()